<header>
  	<%= link_to logo, root_path %>
        <nav class="round">
          <ul>
            <li><%= link_to "Home", root_path %></li>
            <% if signed_in? %>
            <li><%= link_to "Profile", user_path(current_user) %></li>
            <li><%= link_to "Settings", edit_user_path(current_user) %></li>
            <% # the above profile line can also be written  as %>
            <% #<%= link_to "Profile", current_user, we implemented current_user varaible in the contxt of class(self) that we can use that in here(views):D understood?
			%>          
            <% end %>
            <li><%= link_to "Help", help_path %> </li>
            <% if signed_in? %>
            <li><%= link_to "Sign Out", signout_path %></li>
            <% else %>
            <li><%= link_to "Sign In", signin_path %></li>
            <% end %> 
          </ul> 
        </nav>
 </header>