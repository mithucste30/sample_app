<% if object.errors.any? %>
	<div id='error_explanation'>
		<h2>
		<%= pluralize(object.errors.count, 'error') %>
		probihited this <%= object.class.to_s.underscore.humanize.downcase %>  from being saved:
		</h2>
	    <p>There was problem in the following field:</p>
		<ul>
			<% object.errors.full_messages.each do |message| %>
			<li><%= message %></li>
			<% end %>
		</ul>
	</div>
<% end %>